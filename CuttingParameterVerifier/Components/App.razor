<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    @*
      Plain wwwroot paths (not @Assets fingerprints): some shared hosts / IIS setups serve
      fingerprinted or precompressed static-asset endpoints incorrectly, so styles never apply
      even though the app runs. These files exist at the same paths after dotnet publish.
    *@
    <link rel="stylesheet" href="lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="CuttingParameterVerifier.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>

<body>
    <Routes />
    <ReconnectModal />
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.6/dist/chart.umd.min.js" crossorigin="anonymous"></script>
    <script src="js/verifierCharts.js"></script>
    <script src="_framework/blazor.web.js"></script>
</body>

</html>
